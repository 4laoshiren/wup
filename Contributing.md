命名：
- 变量名方法名均采用蛇形命名
    - 方法名需要按照`名词_动词_形容词`方式命名，如`pkg_update`、`pkg_check_outdated`

建议prettierrc采用项目配置：
- 以保持代码换行与项目一致，此时结构最为清晰

使用execa来执行shell命令，使用时永远使用模板字符串
src/managers下的模块不能简单用包管理器名称命名，项目统一在包管理器名称前加上-，因为execa执行shell命令时，默认会优先从本地执行，会将js模块文件当成包管理器尝试运行